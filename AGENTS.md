# Agent instructions

Use **pnpm** for all package and script commands in this repo.

```bash
pnpm install
pnpm dev
pnpm build
pnpm db:migrate
pnpm db:seed
```

Do not use `npm` or `npx` for project scripts.
